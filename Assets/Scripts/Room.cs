using UnityEngine;

public class Room : MonoBehaviour
{
    // private void OnEnable()
    // {
    //     //Hier checken we voor wanneer de speler de ruimte verlaat, dan zetten we em uit en returnen we 
    //     Invoke("Disable", 10f); // Zet de kamer na 10 seconden terug in de pool
    // }
    //
    // private void Disable()
    // {
    //     PoolManager.Instance.ReturnToPool(gameObject.name, gameObject);
    // }
}
